const arr = [10, 20];
console.log(arr.pop());

// push() : 제일 마지막 요소 넣기
// pop() : 제일 마지막 요소 가져오기

const file = "aa.jpg";
// 
console.log(file.split(".").pop());

const uuid = crypto.randomUUID();
console.log(uuid);